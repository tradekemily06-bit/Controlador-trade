# Candle evaluator

Reads completed, validated candles and returns a broker-independent `AnalysisResult`. It is DEMO/read-only and has no order execution responsibilities. It is a foundation for progressively adding the project's trading concepts and confirmation rules.