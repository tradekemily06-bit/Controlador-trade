# P83 — decisão de validação

Converte o resultado factual P82 em decisão explícita: `VALIDATED`, `REJECTED` ou `INCONCLUSIVE`.

Somente `POSITIVE` pode receber `VALIDATED`; a decisão não executa novamente o teste e não altera estratégia, score, risco ou execução.
