# P68 — disposição controlada

Define uma disposição explícita para o ciclo consolidado: `RETAIN`, `REVIEW` ou `RETIRE`.

A disposição é administrativa e factual; não significa que a adaptação foi eficaz e não autoriza execução automática. Imutável, determinística e fail-closed.
